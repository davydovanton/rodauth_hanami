<!DOCTYPE html>
<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
<link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
<title>Hanami rodauth Demo - <%= @page_title %></title>
<style>
  .error {border: 1px red solid;}
</style>
</head>

<body>
  <nav class="navbar navbar-default" role="navigation">
    <div class="container">
      <a class="navbar-brand" href="/">Hanami Rodauth Demo</a>

        <% if rodauth.logged_in? %>
          <form action="/logout" class="navbar-form pull-right" method="post">
            <%== csrf_tag %>
            <input class="btn btn-primary form-control auth-button" type="submit" value="Logout" />
          </form>
        <% end %>
      </div>
    </div>
  </nav>

  <div class="container">
    <% if flash[:notice] %>
      <div class="alert alert-success"><p><%= flash[:notice] %></p></div>
    <% end %>
    <% if flash[:error] %>
      <div class="alert alert-danger"><p><%= flash[:error] %></p></div>
    <% end %>
    <h1><%= @page_title %></h1>

    <%== yield %>
  </div>
</body>
</html>
